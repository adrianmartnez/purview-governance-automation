[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "purview-governance-automation"
dynamic = ["version"]
description = "Microsoft Purview governance automation foundation: installable Python package and CLI shell. Purview API clients are not implemented yet."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.12"
authors = [{ name = "fgnfmackk" }]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.6",
    "build>=1.2",
]

[project.scripts]
purview-governance = "purview_governance.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "purview_governance.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "api_contract: offline contract-lane harness readiness (not Purview API validation)",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
